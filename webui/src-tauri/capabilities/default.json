{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permite ao app subir o backend Python como sidecar.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "binaries/brainbridge-server", "cmd": {}, "args": true }]
    }
  ]
}
